query subspaces($spaceID: UUID_NAMEID!) {
  space(ID: $spaceID) {
    subspaces {
      id
      nameID
      profile {
        displayName
        visuals {
          name
          id
        }
      }
      community {
        id
      }
      collaboration {
        id
      }
    }
  }
}
